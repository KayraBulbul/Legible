import type { PageFolder } from "@/types";

/** Fixture data for the mock branch of src/api/folders.ts. */
export const MOCK_FOLDERS: PageFolder[] = [
  { id: "f1", name: "Reading list" },
  { id: "f2", name: "WCAG research" },
  { id: "f3", name: "Client handoff" },
];
